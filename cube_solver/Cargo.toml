[package]
name = "cube_solver"
version = "0.1.0"
edition = "2024"

[lib]
name = "cube_solver"
crate-type = ["rlib"]

[features]
default = ["std-env"]
std-env = []

[profile.release]
panic = "abort"

[dependencies]


[dev-dependencies]