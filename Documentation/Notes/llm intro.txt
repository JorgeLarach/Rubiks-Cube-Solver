You are building a Rubik’s Cube solving robot using an STM32 Nucleo F401RE microcontroller for hardware control and FreeRTOS, Python for the GUI and initial cube configuration input, and Rust on the MCU for the solver logic only. The MCU C code handles stepper control, timers, and motion execution, while the Rust crate provides only the solver algorithm and does not touch GPIO or hardware. The high-level architecture is: Python GUI for cube input and solver selection, MCU C for motion and stepper control, and Rust for computing the move list. You created a Rust crate called cube_solver with cargo new and successfully ran cargo test. In Rust you defined a Move enum with values U, Ui, U2, D, Di, D2, L, Li, L2, R, Ri, R2, F, Fi, F2, B, Bi, B2. You defined a Cube struct with stickers: [u8; 54]. You wrote a solve_internal stub that checks output length, fills out the first four moves, and returns 4. You wrote an extern "C" function solve_cube that takes raw pointers and converts them into a Cube and a mutable slice for output, then calls solve_internal. You learned that in Rust 2024 you must use #[unsafe(no_mangle)] on functions you want to expose to C, because no_mangle is now an unsafe attribute and the compiler rejects plain #[no_mangle]. You also learned that you must treat raw pointers as unsafe and use core::slice::from_raw_parts_mut to create a Rust slice from a C pointer and length. You built the Rust crate with cargo build --release and produced a static library file libcube_solver.a in target/release. You correctly configured Cargo with [lib] name = "cube_solver" crate-type = ["staticlib"] to produce the .a file. You created the C header file cube_solver.h containing the matching Move enum and solve_cube declaration, and placed it in the STM32 project. You integrated the Rust static library into the STM32CubeIDE project by adding the library search path (-L) pointing to Middlewares/cube_solver and adding -lcube_solver to the linker libraries (-l). You verified that the Rust symbol exists in the library using arm-none-eabi-nm libcube_solver.a | grep solve_cube and confirmed it prints solve_cube. You added a test call in main() before vTaskStartScheduler to prove the C ↔ Rust boundary, using uint8_t cube[54] = {0}; Move moves[16]; size_t n = solve_cube(cube, moves, 16); (and you ignored the returned n). The build initially showed a linker warning that the .note.GNU-stack section was missing and implied an executable stack, and the IDE treated this as a build failure. You experimented with fixing the PATH so arm-none-eabi-gcc could be found when running make, but the underlying issue was the linker executable-stack warning. You fixed it by adding the linker flag -Wl,-z,noexecstack, which explicitly marks the stack as non-executable and removes the warning, allowing the project to build successfully. After this fix the build is clean and the Rust solver now links properly with the STM32 project. Next steps are to pass real cube sticker data from the Python GUI into the MCU, call solve_cube to generate a move list, and feed that move list into the existing FreeRTOS motion task to drive the stepper motors, then expand the solver beyond the stub to implement a full solving algorithm in Rust. You wrote a shell script that runs cargo build --target thumbv7em-none-eabihf --release and copies libcube_solver.a into the middlewares directory for stm project. shell script was added as a pre-build step. After successfully integrating the Rust solver, we implemented UART communication to receive cube states from a Python GUI, created a modular UART driver (uart_cube) for packet reception, and developed a cube_processor module to coordinate solving and motor execution. The system uses FreeRTOS with a CubeProcessingTask that handles UART data, runs the solver when triggered by a button press, and executes motor moves when triggered by another edge-triggered button press. The architecture cleanly separates concerns: UART handling, cube solving, and motor control, with the cube_processor acting as the central coordinator. Next steps replacing the solver stub with a complete algorithm, and finalizing the Python GUI with tkinter. 